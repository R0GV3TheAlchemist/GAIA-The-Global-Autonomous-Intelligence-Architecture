/**
 * batch-a4.data.ts
 * GAIA-OS Crystal Database — Batch A4
 *
 * Stub — batch data not yet populated.
 * Exported as an empty array so index.ts can import it as a default import
 * without causing TS2306 (File is not a module).
 */

import type { CrystalRecord } from './crystal.schema';

const BATCH_A4: CrystalRecord[] = [];

export default BATCH_A4;
