# GAIA Runtime Dependencies — v0.5

# Core
pydantic>=2.0.0

# API layer
fastapi>=0.110.0
uvicorn>=0.29.0

# HTTP client (node sync)
requests>=2.31.0

# Graph layer (optional — falls back to dict if not installed)
networkx>=3.0

# Testing
pytest>=8.0.0
httpx>=0.27.0    # async test client for FastAPI
