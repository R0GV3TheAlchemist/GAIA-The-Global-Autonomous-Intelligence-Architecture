# GAIA Core Requirements
# v0.2 — Ontology-aware epistemic OS

pydantic>=2.0.0

# Graph layer (optional — falls back to dict if not installed)
networkx>=3.0

# API layer (uncomment when Phase 5 API build begins)
# fastapi>=0.110.0
# uvicorn>=0.29.0

# Testing
pytest>=8.0.0
