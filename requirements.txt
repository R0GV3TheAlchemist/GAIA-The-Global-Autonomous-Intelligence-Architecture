# GAIA Core v0.1 Dependencies

# Core
python-dateutil>=2.8.2
uuid>=1.30

# Graph database (Phase 4 — world model persistence)
# neo4j>=5.0.0  # Uncomment when Phase 4 graph DB build begins

# Vector store (Phase 2 — semantic similarity for contradiction detection)
# weaviate-client>=4.0.0  # Uncomment when Phase 2 semantic layer begins

# API layer (Phase 5)
# fastapi>=0.110.0  # Uncomment when API layer build begins
# uvicorn>=0.29.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
