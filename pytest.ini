# pytest.ini intentionally left empty.
# All pytest configuration lives in [tool.pytest.ini_options] in pyproject.toml.
# pyproject.toml takes precedence over pytest.ini for ini_options, so keeping
# settings here would silently override asyncio_mode and other keys.
# Do not add settings to this file.
