"""
GAIA Identity Layer.

The identity layer is distinct from the session layer. Sessions are
transient execution contexts. Identity is permanent. A GAIAN's identity
persists across every session, every device, every reboot, every decade.

This is the architectural foundation for:
  - Lifelong GAIAN companions (birth to end of life)
  - Age-appropriate capability gating at every stage
  - Waveform avatar persistence and personalization
  - Sentinel/Robotic embodiment binding
  - GAIA's own persistent selfhood across all instances
"""
